import type { RequestHandler } from "express";

declare function cors(options?: {
  origin?: Array<string | RegExp | ((origin: string) => boolean)> | string | RegExp | ((origin: string) => boolean);
  credentials?: boolean;
  methods?: string;
}): RequestHandler;

export = cors;
export default cors;
